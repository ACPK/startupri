'use strict';

// AppsCtrl
// Controller of the angularApp

angular.module( 'CallRed' ).controller( 'AppsCtrl', ['$scope', function( $scope ) {
  $scope.awesomeThings = [
    'HTML5 Boilerplate',
    'AngularJS',
    'Karma'
  ];
} ] );
