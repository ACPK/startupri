'use strict';

// BusinessCtrl
// Controller of the angularApp

angular.module( 'CallRed' ).controller( 'BusinessCtrl', ['$scope', function( $scope ) {
  $scope.awesomeThings = [
    'HTML5 Boilerplate',
    'AngularJS',
    'Karma'
  ];
} ] );
