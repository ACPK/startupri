'use strict';

// HiringCtrl
// Controller of the angularApp

angular.module( 'CallRed' ).controller( 'HiringCtrl', ['$scope', function( $scope ) {
  $scope.awesomeThings = [
    'HTML5 Boilerplate',
    'AngularJS',
    'Karma'
  ];
} ] );
