'use strict';

// AboutCtrl
// Controller of the angularApp

angular.module( 'CallRed' ).controller( 'AboutCtrl', ['$scope', function( $scope ) {
  $scope.awesomeThings = [
    'HTML5 Boilerplate',
    'AngularJS',
    'Karma'
  ];
} ] );
