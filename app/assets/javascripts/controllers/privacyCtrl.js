'use strict';

// PrivacyCtrl
// Controller of the angularApp

angular.module( 'CallRed' ).controller( 'PrivacyCtrl', ['$scope', function( $scope ) {
  $scope.awesomeThings = [
    'HTML5 Boilerplate',
    'AngularJS',
    'Karma'
  ];
} ] );
