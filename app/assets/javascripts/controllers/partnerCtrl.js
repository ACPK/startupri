'use strict';

// PartnerCtrl
// Controller of the angularApp

angular.module( 'CallRed' ).controller( 'PartnerCtrl', ['$scope', function( $scope ) {
  $scope.awesomeThings = [
    'HTML5 Boilerplate',
    'AngularJS',
    'Karma'
  ];
} ] );
